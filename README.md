# Ardublockly 安裝包專案

本專案是用於打包與客製化 **Ardublockly** 視覺化程式設計軟體的工程專案。透過此專案，您可以將網頁前端、Python 後端伺服器與 Arduino 編譯工具鏈整合成一個方便發布的 Windows 安裝程式 (`Setup.exe`)。

---

## 👥 適用對象指引

### 1. 給終端使用者 (學生/創客)
如果您只是想要安裝並使用 Ardublockly：
*   請直接執行發布目錄 (`Output/`) 下的 `Ardublockly_Setup.exe` 安裝檔。
*   安裝後，桌面會出現 **Ardublockly** 圖示，點擊即可啟動。
*   啟動時程式會自動清理快取以確保運作順暢，過程完全靜默無黑窗。

### 2. 給教學者/課程設計者 (Teachers & Educators)
如果您希望修改積木功能或介面以配合課程：
*   **修改介面與積木**: 主要原始碼位於 `ardublockly/` 目錄。
    *   `ardublockly/msg/`: 修改語言與提示文字。
    *   `ardublockly/blockly/`: 自定義 Blockly 行為。
    *   `ardublockly/blocks/`: 新增或修改積木定義。
*   **修改後測試**: 您可以直接執行安裝後的程式來預覽 (需確保您的修改已覆蓋到安裝目錄)，或重新編譯安裝包進行發布。

### 3. 給社群開發者 (Developers)
如果您要維護此專案或推送至 GitHub，請務必閱讀以下建置說明。

---

## 🛠️ 開發者指南：建置與依賴管理

由於 Git 版本控制不適合儲存大型二進位檔案，本專案在 `.gitignore` 中排除了核心執行環境。**您在 Clone 此專案後，必須手動「還原」以下兩個關鍵目錄才能開始工作。**

### 關鍵目錄說明 (`arduexec` 與 `arduino`)

在 `ardublockly/` 資料夾下，有兩個被 Git 忽略的目錄，它們是讓程式能運作的「引擎」：

#### 1. `ardublockly/arduexec/` (應用程式執行環境)
*   **內容**: 包含 **Electron** (用於顯示視窗的瀏覽器核心) 以及 **Compiled Python Server** (`server/start.exe`)。
*   **為何被忽略**: 體積過大 (>150MB) 且為編譯後的執行檔。
*   **維護方式**: 
    *   **前端修改**: 修改 `ardublockly/` 下的 HTML/JS 不需更動此目錄。
    *   **後端修改**: 注意！目前的 `start.exe` 是已打包的 Python 環境。若您需要修改 Python 後端邏輯 (Server Compiler)，您需要取得原始 Python 原始碼，修改後使用 PyInstaller 重新打包成 `start.exe` 並覆蓋此處。

#### 2. `ardublockly/arduino/` (Arduino 編譯工具鏈)
*   **內容**: 包含完整的 Arduino IDE 命令列工具 (`arduino_debug.exe`, `hardware/`, `tools/` 等)。
*   **用途**: Ardublockly 後端會呼叫此處的工具來編譯使用者的積木程式碼並上傳到開發板。
*   **維護方式**: 若需要升級 Arduino 核心或增加新的開發板庫 (Libraries)，請在此目錄操作 (例如使用 Arduino IDE 的 Board Manager 下載新板子支援)，然後將整個資料夾作為「新版工具鏈」更新。

### 🚀 如何設定開發環境 (Setup)

1.  **Clone 專案**:
    ```bash
    git clone <your-repo-url>
    ```
2.  **還原依賴 (Restore Dependencies)**:
    您需要從現有的發行版或備份中，將 `arduexec` 和 `arduino` 資料夾複製回 `ardublockly/` 目錄下。
    *   複製 `arduexec/` -> `ardublockly/arduexec/`
    *   複製 `arduino/` -> `ardublockly/arduino/`
3.  **編譯安裝包**:
    *   安裝 [Inno Setup](https://jrsoftware.org/isinfo.php)。
    *   開啟 `Ardublockly_Setup.iss`。
    *   點擊 "Compile"。
    *   完成後，新的安裝檔將產生於 `Output/`。

---

## ⚙️ 專案技術細節

*   **啟動機制**: 採用 `ardublockly_run.vbs` 作為啟動器，它負責：
    1.  刪除 `appdata\Cache` 與 `GPUCache` (解決 Electron 白畫面問題)。
    2.  啟動 `ardublockly.exe` 並隱藏 console 視窗。
*   **安裝腳本**: `Ardublockly_Setup.iss` 負責打包所有所有檔案，並建立指向 `.vbs` 的捷徑。

## 📚 專案文件 (Documentation)

詳細的技術分析與規格書已整理於 `docs/` 資料夾中：

*   [📅 未來路線圖 (Roadmap)](docs/future_directions.md): 包含 PWA, Web Serial 等技術展望。
*   [📖 運作流程圖解 (Workflow)](docs/workflow_explanation.md): 解說從「拉積木」到「燒錄」的完整流程。
*   [🛠️ Arduino CLI 遷移規格書](docs/arduino_cli_migration_spec.md): 給開發者的遷移技術指引。
*   [🍎 macOS 打包可行性分析](docs/macos_feasibility.md): 跨平台架構重構分析。

## 📅 未來路線圖 (Roadmap Summary)

本專案目前使用 Arduino IDE 1.8 (Portable) 作為編譯核心。為了實現更輕量化與高效能的體驗，我們計畫進行以下升級：

*   **[計畫中] 遷移至 Arduino CLI**:
    *   移除 Java 依賴，大幅縮減安裝包體積 (>100MB)。
    *   提升編譯啟動速度。
    *   改寫後端 Python 邏輯以支援新的 CLI 指令架構。
*   **[計畫中] Local Broker 架構**:
    *   將編譯服務獨立為輕量級背景應用。
    *   支援更多 Web 前端連線。

## 📜 授權與致謝 (License & Credits)

本專案是 **Ardublockly** 的衍生作品 (Derivative Work)。

*   **Original Project**: [Ardublockly](https://github.com/carlosperate/ardublockly)
*   **Original Author**: Copyright (c) 2016 [carlosperate](https://github.com/carlosperate/)
*   **License**: Apache License, Version 2.0

Unless stated otherwise, the source code of this project is licensed under the Apache License, Version 2.0.
Full license text can be found in the [LICENSE](LICENSE) file.
